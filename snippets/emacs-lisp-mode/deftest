# -*- mode: snippet -*-
# contributor: Jason M23
# name: deftest
# key: deftest
# --
(ert-deftest test-${1:func} ()
  "Test ${2:description}."
  (let* ((${3:input} ${4:value})
         (${5:expected} ${6:value}))
    (should (equal ($1 $3) $5))))
